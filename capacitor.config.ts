import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'APILPL240822',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
